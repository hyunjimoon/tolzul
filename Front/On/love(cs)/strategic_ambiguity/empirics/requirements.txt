# Strategic Ambiguity Empirics Pipeline - Required Packages

# Core data processing
pandas>=1.5.0
numpy>=1.23.0
xarray>=2023.1.0  # For NetCDF file I/O (default format)

# NetCDF backend (install ONE of the following):
netcdf4>=1.6.0      # Recommended (faster, requires C libraries)
# h5netcdf>=1.2.0   # Alternative: Pure Python (use if netcdf4 install fails)

# Optional: Parquet support (not needed if using NetCDF)
# pyarrow>=14.0.0
# fastparquet>=2024.2.0

# Statistical analysis
statsmodels>=0.14.0
scipy>=1.10.0
patsy>=0.5.0
scikit-learn>=1.4.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter notebook support (optional, for pipeline_simple.ipynb)
jupyter>=1.0.0
notebook>=6.5.0
ipykernel>=6.20.0

# Testing (dev dependencies)
pytest>=7.4.0
pytest-cov>=4.1.0

# Additional utilities
pathlib2>=2.3.7; python_version < '3.4'
